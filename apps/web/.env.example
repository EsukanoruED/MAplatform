# apps/web — Vite environment template. Copy to `.env` and adjust.
# Only VITE_-prefixed variables are exposed to the browser bundle; never put a
# secret in this file.

# Where the Vite dev server proxies /api and /health. Must match the API's PORT.
VITE_API_PROXY_TARGET=http://localhost:4000
