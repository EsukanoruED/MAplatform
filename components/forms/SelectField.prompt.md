One-sentence: labelled dropdown for bounded choices — site, examination type, certificate validity.

```jsx
<SelectField label="Examination type" placeholder="Select an examination"
  options={['Pre-placement', 'Periodic', 'Return to work', 'Exit']} />
```

Six or more options: use this. Two or three mutually exclusive options: use Radio.
