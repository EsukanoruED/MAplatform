One-sentence: multi-select and consent controls — screening questionnaires, scope-of-service selections, acknowledgements.

```jsx
<Checkbox label="Audiometry" description="Required for sites above 85 dB(A)" defaultChecked />
<Checkbox label="Select all workers" indeterminate />
```
