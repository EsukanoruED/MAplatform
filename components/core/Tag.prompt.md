One-sentence: a neutral metadata label for filters and taxonomies — use Badge for status, Tag for facts.

```jsx
<Tag icon={<Icon name="map-pin" size={12} />}>Jazan Site 4</Tag>
<Tag onRemove={() => clear('occupational')}>Occupational health</Tag>
```
