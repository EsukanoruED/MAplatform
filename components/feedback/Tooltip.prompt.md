One-sentence: names an icon-only control or expands an abbreviation — never holds content the user needs to complete a task.

```jsx
<Tooltip label="Spirometry — lung function"><Badge tone="info">SPI</Badge></Tooltip>
```
