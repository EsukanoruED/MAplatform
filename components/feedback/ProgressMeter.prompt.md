One-sentence: shows how far through a bounded programme something is.

```jsx
<ProgressMeter label="Periodic screening coverage" valueLabel="164 / 200" value={164} max={200} />
<ProgressMeter tone="warning" label="Medication stock — Site 4" valueLabel="38%" value={38} size="lg" />
```
